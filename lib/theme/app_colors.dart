import 'package:flutter/material.dart';

class AppColors {
  // Primary Dental Brand Colors
  static const Color primary = Color(0xFF0284C7); // Dental Blue
  static const Color primaryDark = Color(0xFF0369A1);
  static const Color primaryLight = Color(0xFFE0F2FE);
  static const Color primaryHover = Color(0xFF075985);

  // Soft Teal / Aqua Accents
  static const Color accent = Color(0xFF0D9488); // Fresh Teal
  static const Color accentLight = Color(0xFFCCFBF1);
  static const Color accentMuted = Color(0xFF14B8A6);

  // Neutral & Canvas Backgrounds
  static const Color background = Color(0xFFF8FAFC); // Clean Canvas
  static const Color surface = Color(0xFFFFFFFF); // Card & Modal White
  static const Color surfaceMuted = Color(0xFFF1F5F9); // Light Gray Backgrounds
  static const Color surfaceHover = Color(0xFFF8FAFC);

  // Borders & Dividers
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);
  static const Color borderDark = Color(0xFFCBD5E1);

  // Typography & Text
  static const Color textPrimary = Color(0xFF0F172A); // Dark Slate / Navy
  static const Color textSecondary = Color(0xFF475569); // Muted Slate
  static const Color textMuted = Color(0xFF94A3B8); // Light Slate
  static const Color textLight = Color(0xFFFFFFFF);

  // Status & Workflow Badges
  // Scheduled / Confirmed
  static const Color statusScheduledBg = Color(0xFFE0F2FE);
  static const Color statusScheduledText = Color(0xFF0369A1);

  // Arrived / Checked In / Waiting
  static const Color statusWaitingBg = Color(0xFFFEF3C7);
  static const Color statusWaitingText = Color(0xFFB45309);

  // In Consultation / With Doctor
  static const Color statusInConsultationBg = Color(0xFFEDE9FE);
  static const Color statusInConsultationText = Color(0xFF6D28D9);

  // Completed / Paid
  static const Color statusCompletedBg = Color(0xFFD1FAE5);
  static const Color statusCompletedText = Color(0xFF047857);

  // Cancelled / No Show / Overdue
  static const Color statusCancelledBg = Color(0xFFFFE4E6);
  static const Color statusCancelledText = Color(0xFFBE123C);

  // Partial / Pending
  static const Color statusPendingBg = Color(0xFFFFEDD5);
  static const Color statusPendingText = Color(0xFFC2410C);

  // Doctor Availability Indicators
  static const Color doctorAvailable = Color(0xFF10B981);
  static const Color doctorConsulting = Color(0xFF8B5CF6);
  static const Color doctorBusy = Color(0xFFF59E0B);
  static const Color doctorBreak = Color(0xFF64748B);
  static const Color doctorUnavailable = Color(0xFFEF4444);

  // Action Buttons
  static const Color callGreen = Color(0xFF10B981);
  static const Color whatsappGreen = Color(0xFF25D366);
  static const Color smsBlue = Color(0xFF0284C7);
}
